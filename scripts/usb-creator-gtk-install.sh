#!/bin/bash

# install usb-creator-gtk if not already installed
# usb-creator-gtk is the default USB creator tool in Ubuntu
# this allows to easily create bootable USB sticks from ISO files
sudo apt install usb-creator-gtk